body > header {
    position: sticky;
    top: 0;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
    z-index: 999;
    background-color: white;
}
body > header svg,
body > header img,
body > main > header img {
    width: 3rem;
    bottom: -0.3rem;
    margin-right: 0.2rem;
}
body > header svg,
body > header img {
    bottom: -0.3rem;
    position: relative;
}
body > header {
    position: sticky;
    top: 0;
    background-color: white;
    border-bottom: 1px solid var(--pico-muted-border-color);
}
body > header {
    padding-top: 0;
    padding-bottom: 0;
}

@media (min-width: 1200px) {
    aside.phpdocumentor-element-found-in {
        position: absolute;
        top: 0;
        right: var(--spacing-sm);
        font-size: var(--text-sm);
        margin-bottom: 0;
    }
}
